<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.kdg.scoutsappadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_beer_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".roddelDetailActivity" />
        <activity android:name=".addRoddelActivity" />

        <activity
            android:name=".LoadingScreenActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".roddeldetailzonderphotoactivity"
            android:label="roddelDetailZonderPhotoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".StreepkeActivity"
            android:label="@string/title_activity_streepke"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ui.addPeriodeActivity"
            android:label="@string/title_activity_add_periode"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>